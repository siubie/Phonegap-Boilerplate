/**
 * App initial class
 * @author kmil0cv
 */
App = function() {
	
	/**
	 * main function,  entry point of the application
	 * executes only when the device is ready
	 * @author kmil0cv
	 */
	this.init = function() {
		alert('Device Ready!');
	}
}
